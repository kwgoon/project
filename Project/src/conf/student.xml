<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="student">
	<select id="selectAll" resultType="student">
		SELECT * FROM students
	</select>
	<select id="selectOne" parameterType="String" resultType="student">
		SELECT * FROM students WHERE code=#{code}
	</select>
	<insert id="insertOne" parameterType="student">
		INSERT INTO students VALUES (seq_code.nextval, #{title}, #{type}, #{content}, #{days}, #{time}, #{userId})
	</insert>
	<update id="updateOne" parameterType="student">
		UPDATE students 
		SET name=#{name}, company=#{company}, email=#{email} 
		WHERE code=#{code}
	</update>
	<delete id="deleteOne" parameterType="String">
		DELETE FROM students WHERE code=#{code}
	</delete> 
</mapper>